---
---

<section class="contact-form-section">
  <div class="contact-form-container">
    <div class="contact-form-header">
      <p class="contact-form-label">PREMIÃˆRE Ã‰TAPE</p>
      <h2>Une conversation de 30 minutes.<br/>Sans engagement.</h2>
      <p class="contact-form-intro">DÃ©crivez votre situation en quelques lignes. Je vous rÃ©ponds sous 48h pour convenir d'un moment.</p>
    </div>

    <div class="contact-form-grid">
      <form
        id="contact-form"
        action="https://formspree.io/f/mbderpke"
        method="POST"
        novalidate
      >
        <div class="form-row">
          <div class="form-group">
            <label for="prenom">PrÃ©nom *</label>
            <input type="text" id="prenom" name="prenom" required placeholder="Votre prÃ©nom" />
          </div>
          <div class="form-group">
            <label for="nom">Nom *</label>
            <input type="text" id="nom" name="nom" required placeholder="Votre nom" />
          </div>
        </div>

        <div class="form-group">
          <label for="email">Email *</label>
          <input type="email" id="email" name="email" required placeholder="votre@email.fr" />
        </div>

        <div class="form-group">
          <label for="fonction">Fonction</label>
          <input type="text" id="fonction" name="fonction" placeholder="Ex : Directeur d'Ã©tablissement, DRH, Manager..." />
        </div>

        <div class="form-group">
          <label for="message">Votre situation *</label>
          <textarea id="message" name="message" required rows="5" placeholder="DÃ©crivez briÃ¨vement ce que vous traversez et ce que vous cherchez..."></textarea>
        </div>

        <input type="text" name="_gotcha" style="display:none" />

        <button type="submit" class="form-submit" id="form-submit-btn">
          <span class="btn-text">Envoyer ma demande</span>
          <span class="btn-loading" style="display:none">Envoi en cours...</span>
        </button>

        <p class="form-mention">* Champs obligatoires Â· Vos donnÃ©es restent strictement confidentielles</p>
      </form>

      <div class="contact-infos">
        <div class="contact-info-item">
          <span class="contact-info-label">TÃ©lÃ©phone</span>
          <a href="tel:+33675747456" class="contact-info-value">06 75 74 74 56</a>
        </div>
        <div class="contact-info-item">
          <span class="contact-info-label">Email</span>
          <a href="mailto:david.delord@m2p-coaching.fr" class="contact-info-value">david.delord@m2p-coaching.fr</a>
        </div>
        <div class="contact-info-item">
          <span class="contact-info-label">DisponibilitÃ©</span>
          <span class="contact-info-value">Visio France entiÃ¨re<br/>PrÃ©sentiel Occitanie</span>
        </div>
        <div class="contact-info-item">
          <span class="contact-info-label">RÃ©ponse</span>
          <span class="contact-info-value">Sous 48h ouvrÃ©es</span>
        </div>
        <div class="contact-info-divider"></div>
        <p class="contact-info-ou">Ou rÃ©servez directement un crÃ©neau :</p>
        <a href="https://calendly.com/david-delord-m2p-coaching/echange-decouverte-30-min-m-p-coaching" target="_blank" class="contact-calendly">
          Je rÃ©serve
        </a>
      </div>
    </div>

    <div id="form-success" class="form-success" style="display:none">
      <p>âœ“ Message envoyÃ©. Je vous rÃ©ponds sous 48h.</p>
    </div>
    <div id="form-error" class="form-error" style="display:none">
      <p>Une erreur s'est produite. Merci de rÃ©essayer ou de me contacter directement par email.</p>
    </div>
  </div>
</section>

<script>
  const form = document.getElementById('contact-form') as HTMLFormElement | null;
  const successMsg = document.getElementById('form-success');
  const errorMsg = document.getElementById('form-error');
  const submitBtn = document.getElementById('form-submit-btn') as HTMLButtonElement | null;
  const btnText = submitBtn?.querySelector('.btn-text') as HTMLElement | null;
  const btnLoading = submitBtn?.querySelector('.btn-loading') as HTMLElement | null;

  if (form && submitBtn) {
    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      if (btnText) btnText.style.display = 'none';
      if (btnLoading) btnLoading.style.display = 'inline';
      submitBtn.disabled = true;
      try {
        const response = await fetch(form.action, {
          method: 'POST',
          body: new FormData(form),
          headers: { 'Accept': 'application/json' }
        });
        if (response.ok) {
          form.style.display = 'none';
          if (successMsg) successMsg.style.display = 'block';
        } else {
          if (errorMsg) errorMsg.style.display = 'block';
          if (btnText) btnText.style.display = 'inline';
          if (btnLoading) btnLoading.style.display = 'none';
          submitBtn.disabled = false;
        }
      } catch {
        if (errorMsg) errorMsg.style.display = 'block';
        if (btnText) btnText.style.display = 'inline';
        if (btnLoading) btnLoading.style.display = 'none';
        submitBtn.disabled = false;
      }
    });
  }
</script>

<style>
  * { box-sizing: border-box; }

  .contact-form-section {
    background: #2C3028;
    padding: 5rem 2rem;
    width: 100%;
    overflow: hidden;
  }

  .contact-form-container {
    max-width: 1100px;
    margin: 0 auto;
    width: 100%;
  }

  .contact-form-header {
    margin-bottom: 3rem;
    text-align: center;
  }

  .contact-form-label {
    font-size: 0.75rem;
    letter-spacing: 0.15em;
    color: #C18E52;
    text-transform: uppercase;
    font-weight: 600;
    margin-bottom: 0.75rem;
  }

  .contact-form-header h2 {
    font-size: clamp(1.6rem, 3vw, 2.2rem);
    font-weight: 700;
    color: #F4F1EA;
    line-height: 1.25;
    margin-bottom: 0.75rem;
  }

  .contact-form-intro {
    font-size: 1rem;
    color: #9aab92;
    line-height: 1.7;
    max-width: 560px;
    margin: 0 auto;
  }

  .contact-form-grid {
    display: grid;
    grid-template-columns: 1fr 280px;
    gap: 4rem;
    align-items: start;
    width: 100%;
  }

  /* Formulaire */
  form {
    width: 100%;
    min-width: 0;
  }

  .form-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
    width: 100%;
  }

  .form-group {
    margin-bottom: 1.25rem;
    width: 100%;
  }

  .form-group label {
    display: block;
    font-size: 0.82rem;
    font-weight: 500;
    color: #c8c4b8;
    margin-bottom: 0.4rem;
    letter-spacing: 0.02em;
  }

  .form-group input,
  .form-group textarea {
    width: 100%;
    max-width: 100%;
    background: rgba(255,255,255,0.06);
    border: 1px solid rgba(255,255,255,0.12);
    border-radius: 3px;
    padding: 0.75rem 1rem;
    color: #F4F1EA;
    font-size: 0.95rem;
    font-family: inherit;
    transition: border-color 0.2s;
    outline: none;
  }

  .form-group input::placeholder,
  .form-group textarea::placeholder {
    color: rgba(255,255,255,0.25);
  }

  .form-group input:focus,
  .form-group textarea:focus {
    border-color: #C18E52;
    background: rgba(255,255,255,0.09);
  }

  .form-group textarea {
    resize: vertical;
    min-height: 130px;
  }

  .form-submit {
    width: 100%;
    background: #C18E52;
    color: #fff;
    border: none;
    padding: 1rem;
    font-size: 1rem;
    font-weight: 600;
    border-radius: 3px;
    cursor: pointer;
    transition: background 0.2s;
    font-family: inherit;
  }

  .form-submit:hover { background: #a97a42; }
  .form-submit:disabled { opacity: 0.7; cursor: not-allowed; }

  .form-mention {
    font-size: 0.75rem;
    color: rgba(255,255,255,0.3);
    margin-top: 0.75rem;
    text-align: center;
  }

  /* Infos contact */
  .contact-infos {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
    padding-top: 0.5rem;
    min-width: 0;
  }

  .contact-info-item {
    display: flex;
    flex-direction: column;
    gap: 0.2rem;
  }

  .contact-info-label {
    font-size: 0.72rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: #C18E52;
    font-weight: 600;
  }

  .contact-info-value {
    font-size: 0.95rem;
    color: #F4F1EA;
    text-decoration: none;
    line-height: 1.5;
    word-break: break-word;
  }

  .contact-info-value:hover { color: #C18E52; }

  .contact-info-divider {
    border-top: 1px solid rgba(255,255,255,0.1);
    margin: 0.5rem 0;
  }

  .contact-info-ou {
    font-size: 0.85rem;
    color: #9aab92;
    margin: 0;
  }

  .contact-calendly {
    display: block;
    border: 1px solid #C18E52;
    color: #C18E52;
    padding: 0.65rem 1.25rem;
    border-radius: 3px;
    font-size: 0.88rem;
    font-weight: 600;
    text-decoration: none;
    text-align: center;
    transition: background 0.2s, color 0.2s;
  }

  .contact-calendly:hover {
    background: #C18E52;
    color: #fff;
  }

  .form-success {
    background: rgba(110, 135, 116, 0.2);
    border: 1px solid #6E8774;
    border-radius: 4px;
    padding: 1.5rem 2rem;
    text-align: center;
    margin-top: 1rem;
  }

  .form-success p { color: #9aab92; font-size: 1rem; margin: 0; }

  .form-error {
    background: rgba(180, 80, 80, 0.15);
    border: 1px solid rgba(180, 80, 80, 0.4);
    border-radius: 4px;
    padding: 1rem 1.5rem;
    margin-top: 1rem;
  }

  .form-error p { color: #e08080; font-size: 0.9rem; margin: 0; }

  /* â”€â”€ RESPONSIVE MOBILE â”€â”€ */
  @media (max-width: 768px) {
    .contact-form-section {
      padding: 3rem 1.25rem;
    }

    .contact-form-grid {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }

    .form-row {
      grid-template-columns: 1fr;
      gap: 0;
    }

    .contact-infos {
      border-top: 1px solid rgba(255,255,255,0.1);
      padding-top: 2rem;
    }
  }
</style>
